import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    marginVertical: 8,
    marginHorizontal: 16,
    padding: 24,
    borderRadius: 16,
  },

  title: {
    fontSize: 20,
    marginBottom: 4,
    color: '#fdf7f5',
  },

  text: {
    color: '#e4dedd',
  },
});

export default styles;
